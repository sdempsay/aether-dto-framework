package fixtures;

import org.dempsay.aether.api.store.AetherSingletonStore;

/** Hand-written stand-in for generated ConfigDtoStore (api codegen). */
public interface ConfigDtoStore extends AetherSingletonStore<ConfigDto> {
}
